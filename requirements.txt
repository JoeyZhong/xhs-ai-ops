PyExecJS
requests
loguru
python-dotenv
retry
openpyxl
aiohttp
opencv-python
numpy
qrcode

# Web API 后端骨架（strangler-fastapi-skeleton 引入，与 Streamlit 共存）
# 启动：python -m uvicorn server.main:app --reload --port 8000
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27         # FastAPI TestClient 依赖（验收脚本用）
sse-starlette>=1.6  # SSE 流式响应
pandas>=2.0         # Excel 落盘（run_search.py 已用，补全声明）
pytest>=8.0
pytest-asyncio>=0.23

# Phase 4a · PostgreSQL 多租户(本机 → 阿里云 RDS)
# 启动 PG:docker compose -f docker-compose.dev.yml up -d
# 跑 migration:python -m db.migration_runner
psycopg2-binary>=2.9   # 同步驱动 + ThreadedConnectionPool(纪律:不用 asyncpg)
PyJWT>=2.8             # §A3 JWT HS256 签发与校验

# Sidecar · ReaJason/xhs 采集引擎（MIT，可商用）
# 自动更新: python sidecar/manage.py update
xhs>=0.2.13